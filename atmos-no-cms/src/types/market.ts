export type Market = {
  id: string;
  slug: string;              // e.g., "corporate-events"
  name: string;              // e.g., "Corporate Events"
  blurb: string;             // short description shown on card
  image: string;             // path to card image
};